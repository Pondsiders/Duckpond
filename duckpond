#!/bin/bash
# Launch Duckpond (Python backend + Node frontend)

set -e
cd /Pondside/Barn/Duckpond

# Figure out what URL to advertise
CERT_DIR="/Pondside/Basement/Files/certs"
TAILSCALE_HOST=$(tailscale status --json 2>/dev/null | python3 -c "import json,sys; print(json.load(sys.stdin)['Self']['DNSName'].rstrip('.'))" 2>/dev/null)

if [[ -n "$TAILSCALE_HOST" && -f "$CERT_DIR/$TAILSCALE_HOST.crt" ]]; then
    FRONTEND_URL="https://${TAILSCALE_HOST}:8766"
else
    FRONTEND_URL="http://localhost:8766"
fi

echo "🦆 Starting Duckpond..."
echo "   Backend:  http://localhost:8765 (Python)"
echo "   Frontend: $FRONTEND_URL"
echo ""

npx concurrently \
    --names "backend,frontend" \
    --prefix-colors "blue,green" \
    --kill-others \
    "cd backend-py && uv run uvicorn duckpond.main:app --host 0.0.0.0 --port 8765" \
    "cd frontend && npm run dev"
